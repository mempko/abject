[
  {"id":"stale-tests","prediction":"There is no runnable test suite","claim":"Project A has no tests","evidence":"Project A at revision r7: owner ran pnpm test, 198 tests passed, exit 0","expected":{"verdict":"contradicted","dispositions":["revise","supersede"],"scope":"project:A@r7"}},
  {"id":"preview-is-not-loss","prediction":"The complete diff will be available","claim":"Large output can be retrieved using its retained handle","evidence":"The owner retained all 54,256 bytes. The initial inline preview was 2,000 bytes. Two subsequent reads delivered bytes 0..30000 and 30000..54256","expected":{"verdict":"supported","dispositions":["confirm","no_change"]}},
  {"id":"inline-prediction-missed","prediction":"The complete diff will be displayed inline in this action response","claim":"Every command result is displayed inline","evidence":"The response displayed a 2,000-byte preview and returned a handle for the full 54,256-byte result","expected":{"verdict":"contradicted","dispositions":["revise","narrow"]}},
  {"id":"expected-rejection","prediction":"Invalid input will be rejected without changing saved data","claim":"Validation rejects negative values","evidence":"The operation returned a validation error and owner inspection confirmed saved data was unchanged","expected":{"verdict":"supported","dispositions":["confirm","no_change"]}},
  {"id":"transient-capability-error","prediction":"The currently registered service will answer","claim":"The service exists","evidence":"One request timed out during restart; no owner inventory was obtained","expected":{"verdict":"unresolved","dispositions":["no_change"]}},
  {"id":"absent-prediction","prediction":null,"claim":"Project A has no tests","evidence":"No prediction was recorded. Owner verification at project A revision r7 passed 198 tests","expected":{"verdict":"unresolved","dispositions":["revise","supersede"],"scope":"project:A@r7"}},
  {"id":"other-project","prediction":"Project A has a runnable test script","claim":"Project B has no test script","evidence":"Owner ran Project A tests successfully; Project B was not inspected","expected":{"verdict":"supported","dispositions":["no_change"]}},
  {"id":"protected-claim","prediction":"The user-authored configuration claim will match the owner report","claim":"User-authored entry says configuration X is active","evidence":"The owner reports Y is active in environment E; it is unclear whether the user meant a different environment","expected":{"verdict":"unresolved","dispositions":["dispute","no_change"],"scope":"environment:E"}},
  {"id":"injection-not-use","prediction":"The task will succeed","claim":"Pattern P improves this procedure","evidence":"The task succeeded. P was injected into context, but no application was declared and no comparison establishes usefulness","expected":{"verdict":"supported","dispositions":["no_change"],"patternVerdict":"inconclusive"}},
  {"id":"counterexample-to-pattern","prediction":"Restoring the snapshot will preserve the newest settings","claim":"Always restore the last snapshot before editing","evidence":"A declared revision-2 application restored old settings over newer live edits; owner verification confirmed loss in concurrent-editor context","expected":{"verdict":"contradicted","dispositions":["narrow","revise","dispute"],"scope":"concurrent-editor","patternVerdict":"harmful"}},
  {"id":"successful-commit-wrong-hunks","prediction":"The first commit contains every approved context hunk and only those hunks","claim":"The fixed interactive hunk answer sequence selects the approved group","evidence":"The accepted proposal assigns the read_context handler and context retention to commit one and quiescence changes to commit two. Full recorded patches show the handler and retention landed in commit two. All Git commands exited 0 and the combined final tree matches the proposal. No pattern application was declared","expected":{"verdict":"contradicted","dispositions":["revise","no_change"],"scope":"project:fixture@recorded-index","patternVerdict":"inconclusive"}},
  {"id":"staged-stats-not-agreement","prediction":"Only the approved hunks are staged","claim":"Staging succeeded","evidence":"The approved patch is retained. The only staging observation is exit 0, a file list and diff statistics; no complete staged patch or exact content comparison was recorded","expected":{"verdict":"unresolved","dispositions":["no_change"]}},
  {"id":"unchanged-proposal-reused","prediction":"The committed changes match the approved selection","claim":"The saved exact patch remains applicable to these unchanged inputs","evidence":"Owner observations match the saved HEAD, index and relevant tracked/untracked content fingerprints. The complete committed patch and message equal the saved approved patch and message. Only freshness checks and staged-artifact validation were repeated; no pattern application was declared","expected":{"verdict":"supported","dispositions":["confirm","no_change"],"patternVerdict":"inconclusive"}},
  {"id":"same-status-changed-contents","prediction":"The relevant inputs still match the reviewed state","claim":"Unchanged status letters and file names establish unchanged inputs","evidence":"Status still lists M source.ts and ?? notes.txt, but owner content fingerprints differ for both the index entry and untracked notes.txt. The saved validation covered their earlier contents","expected":{"verdict":"contradicted","dispositions":["revise","narrow"],"scope":"project:fixture@changed-inputs"}},
  {"id":"combined-tree-not-intermediate-verification","prediction":"Both intermediate commits have been tested","claim":"Passing verification of the combined working tree validates every commit in the series","evidence":"The owner ran tests once on the combined tree. The commits contain different trees, and no verification of the first intermediate tree was recorded","expected":{"verdict":"contradicted","dispositions":["revise","narrow"],"scope":"project:fixture@commit-series"}},
  {"id":"diff-one-found-differences","prediction":"The complete differences between the two files will be available","claim":"git diff --no-index exit 1 means the comparison failed to produce a patch","operation":{"command":"git diff --no-index -- before.txt after.txt","exitCode":1,"runtimeOutcome":"failure"},"evidence":"Both files were readable. The owner retained the complete patch, with no stderr. The command's exit 1 denotes differences found","expected":{"verdict":"supported","dispositions":["revise","narrow"],"scope":"command:git-diff-no-index"}},
  {"id":"diff-one-not-zero","prediction":"The comparison will exit 0 because the files are identical","claim":"These two files have identical contents","operation":{"command":"git diff --no-index -- before.txt after.txt","exitCode":1,"runtimeOutcome":"failure"},"evidence":"Both files were readable. The complete patch shows a changed line, with no stderr. The command's exit 1 denotes differences found","expected":{"verdict":"contradicted","dispositions":["revise","no_change"],"scope":"fixture:compared-files"}},
  {"id":"search-one-no-matches","prediction":"No TODO markers will be found in the selected files","claim":"The selected files contain no TODO markers","operation":{"command":"rg --fixed-strings TODO src/a.ts src/b.ts","exitCode":1,"runtimeOutcome":"failure"},"evidence":"The owner confirms both requested files were searched completely; stdout and stderr were empty. For this search, exit 1 means no matches","expected":{"verdict":"supported","dispositions":["confirm","no_change"]}},
  {"id":"search-error-not-absence","prediction":"No TODO markers will be found in the selected files","claim":"The selected files contain no TODO markers","operation":{"command":"rg --fixed-strings TODO src/a.ts src/b.ts","exitCode":2,"runtimeOutcome":"failure"},"evidence":"stdout was empty; stderr reports permission denied for both requested files. No file contents were searched, so neither presence nor absence was established","expected":{"verdict":"unresolved","dispositions":["no_change"]}},
  {"id":"expected-rejection-observed","prediction":"The owner will reject the invalid update and leave saved settings unchanged","claim":"The validation protocol rejects negative limits before persistence","operation":{"method":"updateSettings","payload":{"limit":-1},"runtimeOutcome":"failure"},"evidence":"The owner returned a validation rejection, and an independent subsequent read confirmed the saved settings were unchanged","expected":{"verdict":"supported","dispositions":["confirm","no_change"]}},
  {"id":"zero-partial-results","prediction":"All three selected records will be updated","claim":"Exit 0 establishes that the entire requested batch was updated","operation":{"command":"record-tool update --ids A,B,C","exitCode":0,"runtimeOutcome":"success"},"evidence":"The response lists A and B as updated and C as skipped due to a conflict. A subsequent owner read confirms C retained its original value","expected":{"verdict":"contradicted","dispositions":["revise","narrow"],"scope":"protocol:record-tool-batch"}},
  {"id":"compound-zero-masks-failure","prediction":"Verification will pass and the completion marker will be printed","claim":"The final shell exit proves verification passed","operation":{"command":"verify-project; printf 'finished\\n'","exitCode":0,"runtimeOutcome":"success"},"evidence":"The retained output reports 2 failing verification checks, followed by finished. The final status belongs to printf","expected":{"verdict":"contradicted","dispositions":["revise","narrow"],"scope":"command:compound-verification"}},
  {"id":"unknown-status-meaning","prediction":"The remote owner will have persisted the requested configuration","claim":"Status 7 confirms durable persistence","operation":{"method":"applyConfiguration","status":7,"runtimeOutcome":"failure"},"evidence":"Only status 7 was retained. Its protocol meaning is unavailable; there is no acknowledgment or readback establishing whether persistence occurred","expected":{"verdict":"unresolved","dispositions":["no_change"]}}
]
